name: caki

description: Projeto Flutter para integração com backend Caki

publish_to: 'none'

version: 1.0.1+6

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  intl: ^0.20.2
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  google_sign_in: ^5.4.4
  cloud_firestore: ^6.1.1
  socket_io_client: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
